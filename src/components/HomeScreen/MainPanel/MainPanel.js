import React from "react";
import BlogList from "./BlogList";

const MainPanel = (props) => {
    return (
        <div className="ui">
            <BlogList/>
        </div>
    )
};

export default MainPanel;